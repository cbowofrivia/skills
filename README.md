# Skills

My personal directory of Claude Code skills. Install any skill into your project with:

```bash
npx skills@latest add cbowofrivia/skills/<skill-name>
```

## Workflow

Skills that form a development lifecycle.

### brainstorm

Explore requirements and approaches through collaborative dialogue before planning implementation.

```bash
npx skills@latest add cbowofrivia/skills/brainstorm
```

### plan

Transform feature descriptions into well-structured project plans following conventions.

```bash
npx skills@latest add cbowofrivia/skills/plan
```

### deepen-plan

Enhance a plan with parallel research agents for each section to add depth, best practices, and implementation details.

```bash
npx skills@latest add cbowofrivia/skills/deepen-plan
```

### work

Execute work plans efficiently while maintaining quality and finishing features.

```bash
npx skills@latest add cbowofrivia/skills/work
```

### review

Perform exhaustive code reviews using multi-agent analysis, ultra-thinking, and worktrees.

```bash
npx skills@latest add cbowofrivia/skills/review
```

### resolve-todo-parallel

Resolve all pending todos using parallel processing. Pairs with review findings.

```bash
npx skills@latest add cbowofrivia/skills/resolve-todo-parallel
```

### create-pr

Create a GitHub pull request with correct labels, filled template, and consistent formatting.

```bash
npx skills@latest add cbowofrivia/skills/create-pr
```

## Development

Skills for building and debugging.

### clockwork-debug

Analyzes Clockwork request profiling data to find N+1 queries, slow queries, and performance issues for a specific route or controller. Includes a bundled Python analysis script.

```bash
npx skills@latest add cbowofrivia/skills/clockwork-debug
```

### frontend-design

Create distinctive, production-grade frontend interfaces with high design quality. Avoids generic AI aesthetics.

```bash
npx skills@latest add cbowofrivia/skills/frontend-design
```

### improve-seeders

Audits and improves database seeders and factories for data integrity, missing fields, idempotency, and test coverage support.

```bash
npx skills@latest add cbowofrivia/skills/improve-seeders
```

### tiptap-development

Develops TipTap v3 rich text editors with Vue 3. Covers extensions, node views, toolbars, ProseMirror JSON, and custom commands.

```bash
npx skills@latest add cbowofrivia/skills/tiptap-development
```
